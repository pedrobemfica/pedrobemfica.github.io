export class FilesView {
    constructor(user_id) {
        this.initializeElements()
        this.updateList(user_id)
    }

    initializeElements() {
    }

    updateList(user_id) {
        
    }
}